# 按鍵配置與 WinUI 3 設定介面

新版開始功能表 → 錦筆劃輸入法 → 錦筆畫輸入法設定。使用 C++/WinRT 與 WinUI 3，不需管理員權限；外觀跟隨 Windows 深淺色。此名稱尚未隨新版安裝到系統，既有 0.3.1 仍顯示舊名稱。

| 位置 | 左／右手 | 標準 | 傳統 |
|---|---|---|---|
| 上左 | Q／U | 點 | 橫 |
| 上中 | W／I | 折 | 豎 |
| 上右 | E／O | 萬用 | 撇 |
| 下左 | A／J | 橫 | 點 |
| 下中 | S／K | 豎 | 折 |
| 下右 | D／L | 撇 | 萬用 |

自訂提供六個筆劃各兩個字母鍵。允許留空；相同筆劃的兩欄可同鍵，但不同筆劃不得重複。
從標準／傳統複製到自訂會取代目前自訂草稿；一般切換模式不會覆蓋自訂內容。
按「套用變更」才存檔；未儲存就關閉會先詢問。自訂時點按鍵方塊再按 A–Z 指定；Delete／Backspace 清除，Esc 取消。試打區使用當前草稿，只預覽筆劃，不查候選字；未綁定按鍵顯示「·」。

## 維護邊界

- `src/windows/storage/layout.hpp`：純 C++ 配置模型、預設、驗證、固定 16-byte SL1 格式；模式與自訂配置一起保存。
- `layout_store.cpp`：Windows 使用者 LocalAppData/StrokeIME/layout.dat。先寫同目錄暫存檔並 FlushFileBuffers，再 MoveFileEx 原子替換；失敗保留原檔。x86/x64 共用同一份。
- `apps/settings/winui`：WinUI 3 圓角卡片、左右手鍵位、即時驗證、試打與儲存提示。以執行緒內按鍵擷取避免輸入法攔截設定字母；內容可捲動，套用按鈕固定在底部。建置與部署見 [winui-settings-build.md](winui-settings-build.md)。
- TSF service：每個啟用的服務透過 message-only window 的 500ms 計時器讀取最新設定，鍵盤回呼不存取設定檔。快取在 idle 才套用，不改動進行中的筆劃或待提交候選。停用時清理計時器和視窗。
- 缺檔採標準模式；損壞／無法讀取時保留最近可用配置，首次啟用則採標準。設定視窗會提示讀取失敗。
- 輸入法更新仍需重開原本載入舊 DLL 的應用程式；0.3.0 載入之後，修改配置不需重啟。
- 不支援操作鍵改綁、六碼模式、詞組；Shift 英文與 Caps Lock 規則不受布局影響。

目前使用完整設定的小檔案輪詢，未加入跨程序通知或設定同步雲端。多個設定視窗同時套用時，以最後一次成功儲存為準。

## 本地學習

開發版新增「學習常用字」開關及「清除學習紀錄…」。開關變更需套用；停用保留紀錄。清除經確認後立即執行。資料在 LocalAppData 的 learning.dat，與 layout.dat 分開。完整行為及限制見 [learning.md](learning.md)。


## 反轉選字鍵順序

開發版提供切換開關「反轉選字鍵順序（9 → 1，首字按 9）」，預設關閉。按套用後，下一次組字生效。第一至第九個候選分別標示／使用 9、8、7、6、5、4、3、2、1；字頻與個人學習排序不變。每一頁都使用相同對應；只有三個候選時為 9、8、7，其餘數字不選字。空白／Enter 仍選第一字，PgUp／PgDn 仍翻頁。恢復預設會關閉反轉。

選字標示與輸入共用 storage/selection.hpp；使用目前組字的 applied_layout，避免設定同步時標示和按鍵不一致。layout.dat 仍為固定 16-byte SL1：第 4 byte 是模式值（0、1、2）加反轉旗標（+3），因此範圍為 0～5。既有設定直接保留按鍵與正常選字順序；未另加舊格式轉換層。


